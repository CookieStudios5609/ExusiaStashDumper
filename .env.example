TOKEN =
PREFIX =
# Image Path: Choose the path ABOVE all the folders that have images in them. The bot recursively checks for
# pictures underneath this directory
IMAGE_PATH =
# This is the delay in seconds the bot will have for posting memes on startup
DEFAULT_DELAY =
# Thi is the channel the bot will automatically post in on startup
DEFAULT_CHANNEL =